{
  "name": "mintlify-multirepo-action",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "prepublishOnly": "npm run clean && npm run build && rm -rf node_modules && npm install --omit=dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@mintlify/models": "^0.0.63",
    "@types/node": "^20.10.8",
    "typescript": "^5.3.3"
  }
}
